services:
  site:
    container_name: content-studio-site
    build: .
    restart: unless-stopped
    expose:
      - "8080"
    networks:
      - content-studio-edge

networks:
  content-studio-edge:
    name: content-studio-edge
    external: true
